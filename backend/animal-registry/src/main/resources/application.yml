server:
  port: 8002

spring:
  application:
    name: animal-registry
  data:
    mongodb:
      uri: mongodb://localhost:27017/animal_registry
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
